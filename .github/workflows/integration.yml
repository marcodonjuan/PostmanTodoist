name: Integration

on: [pull_request]

jobs:
  API_TESTING:
    runs-on: ubuntu-latest
    steps:
      - uses: action/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: '14.x'
      - run: npm run api-tests